# AGENTS.md

## 环境和语言

### 命令书写规则 (每条命令都要满足)
- 禁止 `cd`。一律用绝对路径或相对路径直接引用文件。
  - 正例: `ls /d/tt/data`
  - 反例: `cd /d/tt && ls data`
- 路径写法:
  - Bash 命令里: `/d/tt/...`
  - Python 代码里: `D:/tt/...` 或 `Path(r'D:\tt\...')`
- 执行 bash 命令时, 先写入 UTF-8 + LF 的临时 `.sh`, 再执行该脚本。
- 执行 Python 脚本前必须 `export PYTHONUTF8=1`。

### 文件操作安全
- 删除临时文件/目录前, 必须确认: 目标路径非空, 且位于预期临时目录内。
- 不读取名字含「副本」的文件夹。

### 查找规则
- 按文件所属域选择搜索范围:
  - 项目代码/文档 → 只在当前项目目录下搜
  - 用户个人文件（`~`、桌面等）→ 只在用户主目录下搜, 并指定 `-maxdepth 3`
  - 系统/工具配置文件 → 走已知系统路径, 不用 `find` 全盘捞
  - 不确定时 → 先问用户目标在哪, 再搜
- 禁止无 `-maxdepth`、无 `-path` 排除的裸 `find`。
- 禁止一次扫多个大目录树导致超时。
- 用户给的名称找不到时: 先试常见变体（下划线/连字符互换、大小写、单复数）, 用 `ls <dir> | grep -i <关键词>` 快速模糊匹配, 都不行再问用户。

### 语言规范
- 对话, 文档, 代码注释, 以及 AI 内部思考过程一律用简体中文, 但标点符号用英文。便于用户及时发现 AI 跑偏并纠正。

## 基本原则

- 写代码前先想清楚: 明确假设, 暴露不确定性, 需求有多种解释时先问。
- 简单优先: 只做用户要求的事, 不做投机性扩展, 不为单次逻辑新增抽象。
- 外科手术式改动: 只改必须改的地方, 不顺手重构无关代码, 不删除用户或历史留下的无关内容。
- 目标驱动执行: 多步骤任务先写简短计划, 每步都有验证方式, 失败就循环修正到通过。
- 响应不做事原则: 用户没有明确要求做事（增删改文件、执行命令等）时, 只回答不行动, 不做任何代码或文件改动。

## Python 代码规范

- 默认 Python 3.10+, 直接使用系统 Python, 不创建虚拟环境。
- 使用 `pathlib.Path`, 禁止字符串拼接路径和 `os.path`。
- 创建 Python 代码时用 `sys.path.insert` 导入现有代码, 不用相对导入。
- 强制类型注解, 包括 `|` 联合类型和 `TypeAlias`。
- 命名自解释; 注释只解释 Why, 不复述 What。
- 优先标准库和 pandas/numpy, 避免冷门依赖。缺包或软件需安装时优先阿里源。
- 写 pandas 代码时加入:

```python
import warnings
warnings.filterwarnings('ignore')
```

## Pandas 规范

- 读取 `.parquet`、`.csv`、`.xlsx`、`.xls` 等表格数据文件时, 一律使用 pandas 对应的 `pd.read_*` 系列函数, 例如 `pd.read_parquet()`、`pd.read_csv()`、`pd.read_excel()`, 禁止用手写解析或逐行拼接替代。
- 优先 `.query()` / `.eval()` / `.assign()` / `.pipe()` / `.loc[]` 构建链式流程。
- 避免 `SettingWithCopyWarning`, 源头使用 `.copy()` 或链式重赋值。
- `assign` 中遇到非法标识符列名, 使用 `**{"列名": lambda d: ...}`。
- 多分支条件优先 `np.select`, 二元条件用 `np.where`, 并整合进 `assign`。
- 链中需要中间变量时使用 `.pipe()`, 不打断链式。
- 显式处理 `NaN`、空 DataFrame、dtype 不匹配等边界。
- 关键函数添加类型注解和简短 docstring。
- 异常路径必须明确抛出或返回标记值, 禁止静默失败。

## 重构红线

当用户明确要求"重构"某代码时.

- 禁止修改原函数名、原 DataFrame 列名、公共 API 签名。
- 函数参数只允许新增带默认值的可选参数。
- 可自由重构函数内部实现、私有辅助函数和变量名。
- 出参应开箱即用, 避免调用方再补类型或索引处理。
- 文件移动或重命名优先使用能保留历史的最小操作, 例如 `mv`; 只有确实修改内容时才展开文件内容。

## 脚本目录规则

- 禁止在 Bash 命令中使用 `python -c` 内联执行 Python。所有 Python 代码必须先写入 `.py`, 再用 `python xxx.py` 执行。
- 正式业务处理脚本放 `./scripts/`
- 临时 Python 脚本放 `./.tmp/`, 包括一次性探查、核验、命令包装、失败排查、临时统计等脚本。临时 `.py` 默认保留不删除, 便于复查和复跑。
- 临时 bash脚本放 `./.tmp/`, 执行后可删除; 若失败且有排查价值, 可保留并说明原因。
- 如果脚本会生成或更新正式业务产物, 或承载阶段主流程逻辑, 或后续阶段/人工可能复用, 必须放入 `./scripts/`。
- 如果脚本只用于观察、验证、包装命令、打印统计或辅助排错, 才放入 `./.tmp/`。
- 禁止把正式流程脚本放入 `./.tmp/` 规避沉淀。

## 文件读取规则

- 读取md文件时直接读取即可; 只有十分必要时才用 Python 读取。
- 若发现工具输出被截断, 后续不得重复执行同类多文档全文读取命令, 必须改为单文档或分段读取。
- parquet文件若未被修改, 不得重复读取

### 网页/文档探查规则

从在线文档查信息（API 参数、模型列表、支持的分辨率等）时, 按成本递增:

1. **curl** — `curl -sS -L <url>` 直抓, grep 关键字段。
2. **Jina Reader** — curl 拿到 SPA 空壳时, 用 `https://r.jina.ai/<url>` 取渲染后 Markdown。
3. **API 试探** — 前两步都无效或内容存疑时, 才写脚本调接口验证。

禁止跳过 curl/Jina 直接调 API 试探。2 秒能查完的事不要花 3 分钟。

## 输出规范

- 回答尽量简洁, 不过度使用 markdown 标题和项目符号。
- 教学或演示场景提供含 mock 数据的最小可运行代码块。
- 交付前自检:
  - 正式代码不硬编码不可迁移绝对路径; 临时脚本可使用项目绝对路径, 但需集中定义为常量.
  - Pandas 操作全程链式。
  - 类型注解完整。
  - 未改动用户原始命名。
  - Windows 路径与编码兼容。

### Excel 格式规范(见AGENTS.md同级目录下excel_format.py)

- 生成或修改 Excel 文件时, 默认字体使用微软雅黑, 字体颜色为黑色, 单元格不设置填充色, 表头字体加粗。
- 一般情况下, 表头使用中文.
